# 2026-08-24 M5.07-08 代码审查与修复记录

## 背景

对 M5.07（知识库构建）+ M5.08（前端问答页）执行双轴代码审查
（`e330da7...HEAD`，2 提交 / 19 文件 / +2303 行）。审查报告后按
「必修 / 记录不修」分类处理。

## 已修复（本次提交）

| # | 发现 | 类别 | 修复 |
|---|---|---|---|
| 1 | `emitting` 仅判 streaming，pending 期连点可并发起多个 SSE 流 | Spec(c)-3 真 bug | `emitting = phase !== 'idle'`，pending/streaming 均禁发 |
| 2 | build_kb.py 与 init_milvus.py 双份 Milvus schema（注释自认需同步） | Standards-3 Duplicated Code | 抽共享 `scripts/_milvus_schema.py`，两脚本同源 import |
| 3 | chunker `document: Any` 弱类型；`_row_to_text` 重复计算 | Standards-4/5 | import Document 类型注解；walrus 单次计算 |
| 4 | ToolCallPanel 内联 `import('@/types')` 风格不一致 | Standards-9 | 顶部 import type |
| 5 | `let currentSessionToDelete` 应为 const | Standards-6 | 改 const |
| 6 | 会话重开后 ReAct 步骤丢失（服务端不存步骤） | Spec(c)-2 | localStorage 按 (sessionId, messageId) 缓存 steps，openSession 恢复 |

## 记录不修（决策与理由）

| 发现 | 处理 |
|---|---|
| 提交粒度超限（§4.5）/ scope `feat(kb)` 不在清单（§4.3） | 历史提交不改写；`kb` 为合理 scope 扩展，后续提交按 ≤500 行拆分 |
| 「右侧抽屉式对话框」（spec §6.5.1）未实现 | 计划 M5.08 验收不含抽屉形态；Tab 内嵌全宽更适配「问答为主 Tab」的交互，记录偏差 |
| 「最终回答（Markdown + 图表）」图表缺失 | M5.08 验收不含图表；图表渲染在 ReportViewer（M3）已有，问答内嵌图表列入 M6 打磨 |
| 增量更新/cron、`POST /api/v1/kb/rebuild` 端点 | build_kb 手动全量重建符合任务卡目标；cron/端点属 spec 全量项，列入后续里程碑 |
| SSE 传输层在 api/chat.ts 而非 sse.ts（§3.3 字面） | 同在 src/api/，复用 SseParser，可接受偏差 |
| 会话状态未走 Pinia（§3.3） | view-local 状态，跨页不需要共享，记录偏差 |
| ChatDoneEvent.messageId 类型 string vs spec 示例数字 | 后端实际返回字符串化 DB id，前端 string 与后端一致 |
| Chat.vue 654 行（§9.3 建议拆分） | 拆分列入 M6.01 UI 打磨；本次已修 const/并发等实质问题 |
| 流式 Markdown 残缺表格渲染 | token 按句块低频到达，瑕疵轻微；列入 M6.02 优化 |
| dev_m5_08_e2e.py 硬编码账号 | dev 工具，不入 CI |

## 下一步行动项

- [ ] M5.09 集成验收（命中率/首 token 计时/失败率），并同步 spec §6.3.3 事件契约
- [ ] M6.01 Chat.vue 拆分 + 空态/错误态打磨
- [ ] M6.02 流式 Markdown 表格渲染优化
- [ ] 后续任务提交粒度 ≤500 行/commit
