# 2026-07-20 — M2.06 Extractor 7B 通用 prompt 三表抽取

## 背景

M2.04 已交付 ModelHub（7B GPTQ-Int4 推理 + Scene 路由），M2.05 已交付 VramScheduler + ModelLock（Redis 分布式锁 + LRU 卸载）。
M2.06 在此基础上完成 L3 M7 Extractor：用 7B + JSON Schema 强约束 prompt 从 PP-Structure 还原的 HTML 表格中抽取资产负债表（BS）/ 利润表（IS）/ 现金流量表（CF）的科目数据，输出符合 spec §2.3 M7 schema 的结构化 JSON。

依赖：M2.02（PP-Structure 表格识别输出 HTML）、M2.04（ModelHub.generate）。
验收：输出 JSON 符合 spec §2.3 M7 schema；JSON 解析率 > 90%。

## 决策列表

### D1. Extractor 不持有 model_lock，由 caller 控制

`Extractor.extract(table_html, statement_type, ...)` 只调 `hub.generate(prompt)`，不感知 `VramScheduler` / `ModelLock`。
caller（M2.08 L2 编排或 M2.07+ L3 handler）显式 `with VramScheduler.load_for_scene_with_lock(Scene.EXTRACT) as lock: extractor.extract(...)`。

**理由**：spec §3.9 要求"业务代码不直接 import transformers / vllm"；同理 Extractor 不该耦合显存调度。
**好处**：Extractor 可单测（注入 `_StubHub(ModelHub)` 绕过 `super().__init__`），与 M2.05 lock 解耦；
**代价**：caller 必须显式持锁，M2.08 需在编排文档里写清楚。

### D2. 失败不抛异常，返回 `ExtractionResult(success=False, error=...)`

JSON parse / schema 校验失败时，`_parse` 不抛异常，返回 `ExtractionResult(statement_type=..., statement=None, raw_text=..., error="...", success=False)`。

**理由**：spec §10.3 降级链要求"抽取小模型 JSON 失败 → 重试 1 次 (temp=0.1) → 改用 7B"。M2.06 把失败产物连同 `raw_text` 一起返回，留给 M2.07 validator 用 `build_retry_prompt(error_hint=...)` 重试，再失败才上抛。
**好处**：M2.06 单测能覆盖所有失败路径而不被异常打断；`raw_text` 保留方便 debug。
**代价**：caller 必须 check `result.success`，不能直接 `result.statement`。

### D3. 3 级 JSON 解析回退

`_extract_json_object(text)` 依次尝试：
1. `json.loads(text.strip())` — 模型直接输出纯 JSON
2. `re.search(r"```(?:json)?\s*(\{.*?\})\s*```", text, re.DOTALL)` — ```json fenced block
3. `re.search(r"\{.*\}", text, re.DOTALL)` — prose 嵌入的 `{...}`（greedy）

**理由**：Qwen2.5-Instruct 在 temperature=0.0 下大部分直接输出 JSON，但偶有 fenced 或 prose 嵌入。三级回退确保 spec §M7 的 JSON 解析率 > 90% 验收标准。
**风险**：第 3 级 greedy 正则可能匹配到非目标 JSON（如示例代码块）；M2.07 validator 会再校验 schema 形状，错匹配会被 `_coerce_to_statement` 拒绝返回失败。

### D4. `_coerce_to_statement` 接受 `.value` 或 `.name` 作为 statements key

模型输出 `{"statements": {"balance_sheet": [...]}}`（小写下划线，对应 `StatementType.value`）
或 `{"statements": {"BALANCE_SHEET": [...]}}`（大写，对应 `StatementType.name`）都接受。
非 expected_type 的科目（如 BS prompt 混入 IS 项）静默过滤。

**理由**：7B 模型在 schema hint 下仍可能输出两种 key 形式；过滤非目标表避免 L2 编排串表。
**风险**：过滤可能掩盖模型错误（如 BS prompt 输出全是 IS 项 → 过滤后 statements 空）。
**缓解**：M2.07 validator 检查 `item_count() > 0`，全空则触发 retry。

### D5. `StatementItem.value` 拒绝 NaN + 千分位字符串

- `value: float`，`_reject_nan` validator 用 `math.isnan()` 拒绝 NaN（spec §2.3 M7 数值合法性）。
- 千分位字符串 `"1,234,567"` 显式拒绝并返回失败（pydantic 默认 `int()` 转 `float` 会抛 `ValueError`，被 `_coerce_to_statement` 捕获）。

**理由**：spec §2.3 M7 要求数值合法性；千分位是 7B 偶发输出，留给 M2.07 retry 或上层转换。
**注意**：负值（净利润 -1234.56）合法；万元单位（unit="万元"）合法，`value` 保留原数值不强行换算。

### D6. `build_extract_prompt` 用 `<|im_start|>` chat 格式

prompt 结构：
```
<|im_start|>system
{system_prompt}
<|im_end|>
<|im_start|>user
{header}
{schema_hint}
{table_html}
<|im_end|>
<|im_start|>assistant
```

**理由**：spec §4.7 指定 Qwen2.5-Instruct 系列；末尾 `<|im_start|>assistant\n` 让模型直接续写 JSON 而不复述指令。
**注意**：`ModelHub.generate` 当前接受拼接好的 prompt 字符串直接喂给 `model.generate()`；
M2.04 的 `TransformersBackend` 已用 `tokenizer.apply_chat_template` 时会重复包装 → 已在 M2.04 留 TODO。
M2.06 暂时直接传拼接好的字符串，M2.07+ 评估是否需要 backend 层拆分 chat messages。

### D7. `build_retry_prompt(original_prompt, *, error_hint)`

retry prompt 在原 prompt 后追加：`"上次输出存在以下问题：{error_hint}，请修正后重新输出严格 JSON。"`

**理由**：spec §10.3 降级链要求"重试 1 次 (temp=0.1)"。M2.07 validator 检测到 JSON 解析失败后，
用 error_hint（如"未找到 statements 字段"）拼 retry prompt，temp 降到 0.1 重试一次。
**注意**：retry 不破坏 system role，只追加 user 提示；M2.07 实现 validator 时需把 `Extractor.extract(temperature=0.1)` 显式覆盖。

### D8. `FinancialStatement.to_dict()` 用字符串 key

Pydantic v2 `model_dump()` 默认用 enum key（`{StatementType.BALANCE_SHEET: [...]}`），
但下游 JSON 序列化（FastAPI response / RabbitMQ message）要求字符串 key。
`to_dict()` 显式 `statement_type.value` 转 key，与 `_OUTPUT_SCHEMA_HINT` 输出格式对称。

**理由**：避免 FastAPI `json.dumps` 抛 `TypeError: keys must be str, int, float, bool or None, not StatementType`。

### D9. 三表共用一套 prompt 模板，靠 `statement_type` 区分

`build_extract_prompt` 接受 `statement_type: StatementType` 参数，渲染时：
- header 行：`请从下面的「{statement_type.chinese_name}」HTML 表格中抽取所有科目数据。`
- schema hint：`_OUTPUT_SCHEMA_HINT.replace("<statement_type>", statement_type.value)`

**理由**：避免维护三套 prompt；`StatementType.chinese_name` 属性提供中文表名（资产负债表 / 利润表 / 现金流量表）。
**风险**：通用 prompt 在 IS/CF 上准确率可能低于 BS（BS 结构最规整）；M2.07+ 评估后可按表类型拆分 prompt。

## 已完成的 checklist

- [x] 新建 `app/schemas/statement.py`（StatementType / Scope / Period / StatementItem / FinancialStatement / ExtractionResult）
- [x] 新建 `app/modules/extractor/prompts.py`（`_SYSTEM_PROMPT` / `_OUTPUT_SCHEMA_HINT` / `build_extract_prompt` / `build_retry_prompt`）
- [x] 新建 `app/modules/extractor/extractor.py`（`Extractor` 类 + `_extract_json_object` + `_coerce_to_statement`）
- [x] 新建 `tests/test_m2_extractor.py`（49 个测试，含 `_StubHub` + `_bs_payload` helper）
- [x] `ruff check app/ tests/` 全绿
- [x] `black app/ tests/` 全绿
- [x] `pytest tests/ -q --cov=app` 全套 226 测试全过，整体覆盖率 94%
- [x] 更新 `docs/progress/m2.md` 打勾 M2.06 + 追加交付说明
- [x] 写本决策文档

## 发现的风险

1. **7B 模型实际输出未验证**：M2.06 单测全部用 `_StubHub` mock 模型输出，真实 7B 在 RTX 4050 Mobile 6GB VRAM 上的 JSON 解析率未验证。M2.12 集成测试需用 10 份真实年报跑解析率统计，目标 > 90%。
2. **千分位字符串处理策略**：M2.06 直接拒绝千分位字符串，但 7B 在大数字（如营业收入 1,234,567,890 元）上可能高频输出千分位。M2.07 validator 可考虑在 retry 前 strip 逗号再 parse，而非直接 retry。
3. **`_extract_json_object` 第 3 级 greedy 正则风险**：prose 嵌入场景下可能匹配到示例代码块而非真实输出。M2.07 validator 用 schema 校验兜底，但若模型输出"示例：{...} 实际：{...}"，会匹配到第一个。
4. **chat template 重复包装**：M2.06 拼接 `<|im_start|>...<|im_end|>` 字符串直接喂 `model.generate()`，但 M2.04 `TransformersBackend` 用 `apply_chat_template` 会重复包装。M2.07+ 需评估是否在 backend 层拆分 chat messages，或在 Extractor 层不拼 chat template 直接传 messages list。
5. **未集成 MQ consumer**：M2.06 只交付 Extractor 类 + 单测，未动 `app/mq/consumer.py`。M2.07+ 的 extract handler 需显式 `with VramScheduler.load_for_scene_with_lock(Scene.EXTRACT) as lock: extractor.extract(...)`，busy 时抛 `ModelLockBusyException` → handler 转 `nack(requeue=True)` + sleep（spec §3.9）。

## 下一步行动项

- M2.07：实现 validator，用 `build_retry_prompt(error_hint=...)` + temp=0.1 重试一次；统计 JSON 解析率指标。
- M2.07：评估千分位字符串 strip 策略（retry 前 strip 逗号 vs 直接 retry）。
- M2.08：L2 三表并行抽取编排，在编排层显式 `VramScheduler.load_for_scene_with_lock(Scene.EXTRACT)` 持锁调 Extractor。
- M2.08：评估是否需要按表类型拆分 prompt（BS/IS/CF 各一套）以提升准确率。
- M2.12：集成测试用 10 份真实年报验证 JSON 解析率 > 90%，统计 BS/IS/CF 各自 F1。
