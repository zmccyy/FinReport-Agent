# 实现计划与实际代码库同步更新

- 日期：2026-07-19
- 范围：实现计划（plan）+ 设计文档（spec）与当前代码库状态对齐

## 背景

M1 阶段 17 个任务在 2026-07-19 已全部完成并通过重新验收。然而实现计划文档仍标注为"待评审"状态，且多处与实际实现存在差异——部分差异是 M1 期间不可避免的设计偏离（如 Java 17→21），部分差异是计划的前瞻性描述（如标注尚未实现的 M2-M6 脚本）。本次同步将计划文档、设计文档与代码库实际状态对齐，确保后续里程碑能从准确的基准出发。

## 决策

1. **Java 版本从 17 修正为 21**（plan §2.1）。代码库中 pom.xml、CLAUDE.md、AGENTS.md 均指定 Java 21，pom.xml 中 `<java.version>21</java.version>` 为权威来源。
2. **文档头元数据更新**：plan 版本升至 v1.1，日期更新为 2026-07-19，状态由"待评审"改为"M1 已完成，M2 待开始"；spec 状态由"设计评审中"改为"M1 已实现，M2-M6 待开发"。
3. **目录结构同步**：plan 的 4 个目录树（backend/frontend/scripts/deploy）与实际文件系统对齐。新增的包（trace/）、重构的 domain 子包、实际存在的脚本与模板文件均已反映；M2-M6 的待实现脚本保留但标注状态。
4. **Flyway 迁移计数**：从 V1-V5 更新为 V1-V6（新增 `V6__m1_reliability_hardening.sql`）。
5. **M1 验收状态**：5 条验收标准全部从 `[ ]` 改为 `[x]`，添加完成状态横幅并链接到进度记录。

## 实施清单

- [x] plan §2.1 Java 版本 17→21
- [x] plan §1.2 backend 目录树：添加 trace/、Flyway V6、domain 子包、额外配置类
- [x] plan §1.4 frontend 目录树：添加 assets/、types/、实际存在的视图与组件
- [x] plan §1.5 scripts 目录树：列出实际脚本 + 标注待实现
- [x] plan §1.6 deploy 目录树：更新容器数、添加实际文件、标注待实现
- [x] plan §3.2 M1.03 Flyway V1-V5→V1-V6
- [x] plan §3.2 M1.07 补充缺失配置类与 trace 文件
- [x] plan §3.3 M1 验收全部打勾 + 完成横幅
- [x] plan 页头：版本 v1.1 / 日期 2026-07-19 / 状态更新
- [x] spec 页头：状态由"设计评审中"改为"M1 已实现，M2-M6 待开发"
- [x] 浏览器 E2E 与 CI 流水线均已在 M1 验收中确认通过

## 风险与后续

- 计划中标注为"待实现"的目录和脚本均属于 M2-M6 范围，本次更新仅标注状态，未对后续里程碑做任何预判性修改。
- PyTorch 版本：计划写 2.3.x，实际环境安装 2.5。2.3.x 为最低兼容版本，2.5 向下兼容，故保留原计划版本不变。
- 当前分支 `feature/M1.17-ci-pipeline` 的 CI 流水线提交 `a3e1575` 已通过全部三条 pipeline。
