# 2026-08-21 M4.10 代码审查修复

## 背景

`docs/review/2026-08-20-m4-10-code-review.md` 对 M4.10 抽取质量攻坚三笔提交
（9f442cf / a149264 / b289767）完成 15 项发现（4 高危 / 7 中危 / 4 低危）的
审查。本轮对全部发现逐项读码复核（15/15 属实）并完成修复，报告见
`docs/review/2026-08-21-m4-10-fix-report.md`。

## 决策列表

1. **14 项代码修复，M6 不改码**：`prefetch_count=1` 是 AGENTS.md §8.1 明文
   硬约束（防 API 并发过载），单工作线程是心跳饿死修复的设计，300s 超时是
   质量优先的有意取舍——仅文档记录，SLA 突破留待 spec 变更申请。
2. **M2 检测增强而非改门槛**：粒度过滤（GT 合并+本期）为决策记录标注的有意
   行为，本轮只加覆盖率报告与缺失告警（上期/母公司整体丢失时 WARNING），
   不改 0.9106 验收语义；四粒度 GT 扩建留作后续任务。
3. **H3 采用「base 无默认 + dev overlay 保留默认」**：单独跑 base compose 属
   类生产用法应 fail-loud；sanctioned 开发路径（base + overlay，AGENTS.md
   §7.1）不受影响。不把开发默认值加入校验器黑名单（会误伤开发栈）。
4. **normalize 抽共享模块**（`app/modules/extractor/normalize.py`）：消除
   handler / eval / rebuild 三份拷贝分叉（L2 根因），并为「规范化先于校验」
   （L3）提供无循环导入的引用方向。
5. **H4 预扫退化而非移除过滤器**：无锚点文档退回全页识别（旧行为）+
   warning，相对「零识别 + 根因不可见」是更优失败模式；接受无文本层大型
   扫描件的 OOM 回归（即旧行为），生产 enable_ocr=False 不变。
6. **M5 重试策略不对称**：首抽 AiException（截断）转失败结果走 0.1 温度
   重试（恢复挽回机会）；重试轮仍异常则上抛终态 FAILED——刻意不返回
   success=false 假成功（会触发 H2 同款「步骤 SUCCESS 但无数据」病理）。
7. **M1 宁缺勿错**：降级重建改严格千分位正则 + 熔合行（>30 字符）丢弃，
   接受无千分位小值行退出兜底路径（178 倍虚增即反例证）。
8. **H2 读取侧过滤实现自愈**：脏缓存条目视为 miss 走 MQ 重抽，新 success
   结果覆盖写同 key；无需部署时手工清 Redis。

## 已完成 Checklist

- [x] 高危 4 项（H1 段定位 / H2 缓存读侧 / H3 JWT fail-loud / H4 页过滤）
- [x] 中危 6 项修复 + M6 取舍说明（M1-M5、M7）
- [x] 低危 4 项（L1 eval 容错 / L2 单源化 / L3 校验前规范化 / L4 GT 重建）
- [x] 新增回归测试 16 个（Python 15 + Java 1）
- [x] 全量验证：ai-service 544 passed / backend 340 passed / ruff 通过
- [x] GT 重建 diff 恰好一行（`信用减值损失损失以-` → `信用减值损失`）
- [x] 修复报告 `docs/review/2026-08-21-m4-10-fix-report.md`
- [x] 按关注点拆 7 个 commit 推送 origin/main（de1e592..52a3f48）

## 发现的风险（残余）

- M6：单 worker 300s 挂起仍会阻塞后续消息，缓解路径为扩 ai-service 副本
- H4：无文本层大型扫描件全页识别的 OOM 风险回归（旧行为，已注释记录）
- M4：原投递完成后、ack 失败后的重投仍可能重复执行一次（窗口极小，L2
  幂等键兜底）
- M2：上期/母公司契约回归现有告警可见但不会判 FAIL，彻底解法是四粒度
  GT 扩建

## 下一步行动项

1. M5.01 起启动 Agent 里程碑（ReAct 循环 + 6 工具 + SSE 问答透传）
2. 四粒度 GT 扩建（母公司/上期），F1 门槛升级为四格分别达标
3. F1 复测在真实 E2E 环境重跑一次（本轮修复以单测验证，真实链路复测
   需 dev compose 栈 + LLM_API_KEY）
