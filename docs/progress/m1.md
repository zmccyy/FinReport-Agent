# M1 进度：基础设施与骨架打通（Week 1-2）

> **目标**：Docker Compose 8 service + 骨架链路（mock L3）
> **开始**：2026-07-14
> **预计完成**：2026-07-28

## 开工准备（pre-M1）

- [x] Git 初始化 + .gitignore + .editorconfig
- [x] 目录结构（含 .gitkeep）
- [x] 环境验证：Python 3.11.15 / PyTorch 2.5 / bitsandbytes 0.49.2 / CUDA 12.1
- [x] 4-bit 量化在 Windows 上可用（关键阻塞已解除）
- [x] C6 idempotency_key 修复
- [x] 任务状态机补充（spec §3.2.1）
- [x] 模型下载脚本（scripts/download_models.py）
- [x] 测试年报 PDF 下载指引（data/benchmark/README.md）
- [x] JDK 21 已安装（21.0.11）
- [x] Docker Desktop 已安装运行（29.6.1 + Compose v5.3.0）
- [ ] 下载 Qwen2.5-7B-Instruct GGUF 4-bit（阻塞 M2.04，M1 期间可并行下载）

## 任务清单

- [x] M1.01 仓库初始化与目录骨架
- [x] M1.02 Docker Compose 编排 8 service
- [x] M1.03 MySQL 建表 + Flyway 迁移
- [x] M1.04 MinIO bucket 初始化
- [x] M1.05 Milvus collection 建立
- [x] M1.06 RabbitMQ 拓扑声明
- [x] M1.07 L2 SpringBoot 骨架
- [x] M1.08 L2 AuthController
- [x] M1.09 L2 TaskOrchestrator 骨架
- [x] M1.10 L2 ProgressConsumer（消费 progress.exchange → SSE 路由）
- [x] M1.11 L2 SSE 端点 /tasks/{id}/stream（含断线重连、心跳）
- [x] M1.12 L2 文件上传接口
- [ ] M1.13 L3 FastAPI 骨架
- [ ] M1.14 L3 RabbitMQ 消费者骨架
- [x] M1.15 前端 Vue3 工程初始化
- [x] M1.16 前端登录页 + 上传页 + 进度卡片
- [ ] M1.17 CI 流水线搭建

## M1.01–M1.12 重新验收状态（2026-07-17）

> 以下门禁已全部通过，故上方 M1.01–M1.12 恢复为完成状态。M1.13 及后续任务仍保持未完成，未被本次基础链路验收覆盖。

- [x] 任务授权、上传幂等/用户隔离、三次 MQ 重试和 Redis SSE 恢复的单元测试已补齐
- [x] 本地 Compose：backend liveness/readiness、frontend、MinIO init、RabbitMQ 拓扑与 Milvus collection/index 已实机验证
- [x] Docker 中 R2DBC 应用的启动期 Flyway 迁移已实机验证：`user_account` 至 `task_step` 和 V1–V6 历史均已创建
- [x] `backend/.\mvnw.cmd test` 全量通过（186 tests）
- [x] `backend/.\mvnw.cmd verify` 全量通过，JaCoCo 80% 核心覆盖率门禁实际生效
- [x] `backend/.\mvnw.cmd checkstyle:check spotbugs:check` 通过（0 Checkstyle violations；0 SpotBugs warnings）
- [x] `backend/.\mvnw.cmd verify -Pintegration`（Testcontainers）通过（Flyway fresh V1→V6 与 V5→V6 upgrade）
- [x] `docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build` 重复启动验收通过：9 个常驻服务 healthy，`minio-init` exited 0
- [x] 注册/登录→上传→progress 全阶段→SSE Last-Event-ID、跨用户拒绝、同 key 并发及取消后新 key 重试的端到端演练完成

## M1.15–M1.16 前端验收状态（2026-07-19）

- [x] M1.15：tsconfig 重构为 create-vue 三文件结构（修复 TS6306/TS6310）；`.eslintrc.cjs` 补齐；`npm ci` / `type-check` / `lint` / `build` 全绿；dev server 启动返回 200
- [x] M1.16：Login（登录+注册）/ Reports（列表+空态）/ ReportUpload（拖拽+50MB/PDF 校验+幂等键）/ TaskProgress + ProgressCard（4 阶段+三表明细+SSE 实时状态）
- [x] fetch-based SSE 客户端：携带 Authorization（EventSource 无法带头）、Last-Event-ID 重连、指数退避、401 刷新后重连；修复旧版监听 `complete` 而非后端实际 `done` 事件的 bug
- [x] 401 单飞刷新重试（axios 拦截器）+ RFC 9457 ApiError 统一错误模型
- [x] 修复关键 bug：isAuthenticated 由 computed（localStorage 非响应式，永远 false）改为手动同步 ref
- [x] Playwright 浏览器 E2E 全通过：注册→上传贵州茅台年报→mock 进度注入→4 阶段进度可视化→完成态→列表状态更新（scripts/e2e_m116.py + scripts/inject_mock_progress.py）


## 阶段验收

- [x] base + dev overlay Compose 一键启动：9 个常驻服务 healthy（含 Milvus 的 etcd 依赖），`minio-init` 成功完成
- [x] 上传 PDF → RabbitMQ 注入 PARSE / 三个 EXTRACT / CHECK / REPORT progress → Redis SSE Last-Event-ID 回放
- [x] 上传 PDF → 前端 SSE 看到 PARSE/EXTRACT/CHECK/REPORT 四阶段进度（M1.16 浏览器 E2E 实证）
- [x] MySQL 中 report、task 与 task_step 记录已实机验证，Flyway schema 版本为 V6
- [x] MinIO 原始 PDF 存储、同用户复用和跨用户隔离已实机验证
- [ ] CI 三条流水线全绿（M1.17，不在本次 M1.01–M1.12 重新验收范围）

## 风险跟踪（从 pre-M1 迁移，原文件已归档删除）

| 风险 ID | 描述 | 严重度 | 缓冲策略 |
|---|---|---|---|
| R1 | M4 微调效果不达 0.85（6GB VRAM 约束） | HIGH | 削减标注量 5k→1.5-2k；用 7B 作 teacher model |
| R2 | 8 容器 + 模型推理内存压力 | MEDIUM | 延迟加载非关键容器；prefetch=1 限流 |
| R3 | ECharts SSR 在 Python 生态兼容性 | MEDIUM | 备选：matplotlib 降级方案 |
| R4 | M3 同比/环比异常检测需要历史数据 | MEDIUM | 预置 3 年基准数据到 knowledge base |

## 备注

- risks 源自 `docs/decisions/2026-07-14-pre-m1-preparation.md`（已删除），重新记录以确保不丢失
