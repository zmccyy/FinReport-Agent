# M6 进度：前端打磨评估与发布（Week 11-12）

## 任务清单

- [ ] M6.01 前端 UI 打磨
- [ ] M6.02 前端性能优化
- [x] M6.03 L2 限流完善
      （ratelimit 包：@RateLimit 注解 + RateLimiter（Redis ZSET 滑动窗口 Lua，
        fail-open）+ RateLimitWebFilter（Order -90，handler 注解解析，429 +
        Retry-After RFC 9457）；应用：登录/注册 5 次/分/IP、刷新 10 次/分/IP、
        上传 3 次/分/用户、问答会话/消息 10 次/分/用户；10 个新单测；
        scripts/load_test.py 压测脚本）
- [x] M6.04 L2 Idempotency 完善
      （idempotent 包：@Idempotent 注解 + IdempotentWebFilter（Order -85，响应
        快照缓存回放 + SETNX 在途锁 409 + 缺 Key 422，fail-open）；应用于
        问答会话创建；上传保留 FileService 业务级幂等不叠加（决策记录）；
        6 单测 + IdempotencyIT（Testcontainers 真 Redis/MySQL，回放+422）；
        修复两 filter 的 HandlerMapping bean 歧义（@Qualifier））
- [x] M6.05 可观测性：Prometheus 指标
      （L2：pom 加 micrometer-registry-prometheus + application.yml 暴露
        /actuator/prometheus + BusinessMetrics（步骤成功/任务终态/步骤耗时
        P95/SSE 活跃/MQ 发布），埋点 TaskOrchestrator/SseEmitterPool/
        TaskMessageProducer；L3：app/core/metrics.py（prometheus_client，
        缺依赖时 no-op 降级）+ main.py HTTP middleware（路由模板防标签
        基数爆炸）+ /metrics 端点，埋点 consumer（StageTimer 六阶段）/
        api_backend（LLM 调用/重试/token）/embedder；deploy/prometheus/
        prometheus.yml 抓取配置；Java 376 + Python 610 全绿，black 已过）
- [x] M6.06 Grafana 看板
      （compose 新增 prometheus（9090，7d 保留）+ grafana（3000，匿名
        Viewer + admin 登录）服务与数据卷；provisioning 数据源/看板自动
        加载；3 个看板 JSON：business（任务成功率/阶段耗时 P95 对 SLA/
        SSE 并发/MQ 发布）、system（L2/L3 HTTP 速率与 P95/JVM 堆/429/
        targets up）、model（DeepSeek 调用/重试/token/耗时 P95、embedding）；
        Counter 命名去 _total 后缀防 _total_total 叠加（L2+L3）；
        compose config 校验通过）
- [x] M6.07 Jaeger 链路追踪
      （jaeger all-in-one 容器（16686 UI / 4317 OTLP，内存 5 万 trace）；
        backend：Dockerfile 构建期获取 otel javaagent 2.10.0，compose 经
        JAVA_TOOL_OPTIONS 挂载（dev overlay 覆盖关闭），pom 加
        opentelemetry-api + TraceIdWebFilter 写 finreport.traceId span
        attribute（Jaeger 可按业务 traceId 检索，与日志对齐；无 agent 时
        no-op）；L3：app/core/tracing.py（OTEL_TRACES_ENABLED 驱动 +
        FastAPI/httpx 自动埋点 + OTLP exporter，依赖缺失降级跳过），
        main.py 挂接；Java 376 + Python 全绿；compose 校验通过）
- [ ] M6.08 端到端评估：30 份基准数据集
- [ ] M6.09 评估报告产出
- [ ] M6.10 README + 部署文档
- [ ] M6.11 Docker Compose 一键启动验证
- [ ] M6.12 演示视频脚本 + 录制
- [ ] M6.13 项目总结博客

## 阶段验收

- [ ] 所有 §7.4.2 评估指标达标
- [ ] `docker compose up` 一键启动
- [ ] 演示视频完成
- [ ] README + 部署文档齐全
- [ ] 项目博客发布
