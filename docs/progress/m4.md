# M4 进度：API 化重构 + 全链路去 mock（Week 7-8）

> 方向变更（2026-08-16）：放弃本地模型训练，切换 DeepSeek API + 自研轻量 RAG。
> 原 15 个微调任务作废，见 [决策记录](../decisions/2026-08-16-m4-pivot-deepseek-api-rag.md)。

## 任务清单

- [x] M4.01 架构变更落档：决策记录 + spec/AGENTS/skills/progress 同步
- [x] M4.02 DeepSeekBackend 实现 + ModelHub API 路由改造 + 单测（mock httpx）
      （13 个 API 后端用例：payload 组装 / json_mode / 429·5xx 重试 / 4xx 不重试 /
      超时不重试 / 重试耗尽 / Key 缺失快速失败 / ModelHub 自动初始化）
- [x] M4.03 数据通路-解析侧：minio put_bytes + parse 产物落 MinIO + 表格识别开启
      （`parsed/{taskId}.json` → finreport-artifacts 桶，7 天生命周期；3 个新单测）
- [x] M4.04 extractor handler 去 mock（MinIO 拉 parse 产物 + 表格筛选 + 三表真实抽取）
      （关键词评分选表 + 母公司降权 + 封面推断报告期 + json_mode 注入 + retried 标记；
        13 个新单测）
- [x] M4.05 数据通路-查询侧：L3 只读 MySQL 客户端 + check handler 去 mock
      （RuleEngine + AnomalyDetector + LLMReviewer 接入，M3.04 契约）
      （ReadOnlyMySqlClient 参数化只读查询 + 同公司上期同比 + 合并/本期快照筛选；
        report 步骤暂留 mock 待 M4.06；11 个新单测；prod 依赖 + PyMySQL）
- [ ] M4.06 generator handler 新增 + report 路由切换（报告+图表+PDF，M3.08 契约）
- [ ] M4.07 BgeSmallEmbedder 实装 + ModelHub.embed()（512 维归一化）
- [ ] M4.08 本地 GPU 栈移除：vram_scheduler/TransformersBackend/依赖/配置 + 测试清理
- [ ] M4.09 部署调整：Dockerfile CPU wheel + compose 注入 LLM_API_KEY + download_models 精简
- [ ] M4.10 model_registry 注册 deepseek-chat + bge-small-zh + 端到端验证
      （真实 PDF → API 全链路 + eval_m2_f1 复测）

## 阶段验收

- [ ] 真实 PDF 上传 → parse（含表格识别）→ API 抽取三表 → 勾稽+异常+LLM 复核 → 报告+图表+PDF，全链路无 mock 数据
- [ ] 抽取质量：eval_m2_f1.py 对真实 API 输出 F1 ≥ 0.85
- [ ] ModelHub.embed() 返回 512 维归一化向量（M5 RAG 前置就绪）
- [ ] ai-service prod 镜像 < 3GB（原约 8GB）
- [ ] CI 全绿（单测全 mock，不需 API key / GPU）
- [ ] 文档全部同步（spec / AGENTS / skills / decisions）

## 架构速记

- LLM：DeepSeek API（deepseek-chat，OpenAI 兼容，httpx 直连，json_mode 支持抽取/复核）
- Embedding：本地 bge-small-zh-v1.5 CPU（512 维，Milvus fin_kb 不重建）
- 数据通路：parse 产物 `parsed/{taskId}.json` 落 MinIO；check/report 只读 MySQL
  （financial_statement / accounting_check / anomaly，写入仍归 L2）
- 移除：TransformersBackend / VramScheduler / ModelLock / Scene.LAYOUT / torch CUDA 依赖
