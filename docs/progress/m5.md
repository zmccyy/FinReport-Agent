# M5 进度：Agent 问答与知识库（Week 9-10）

> 2026-08-23 Phase 1（M5.01-03）完成。方向修正（2026-08-16 pivot 后）：
> M5.03 的 outlines/guidance 方案废弃，改为 DeepSeek json_mode + tool_call
> JSON Schema 注入（与 M4 架构一致）；知识库验收口径放宽为 3 份 sample
> 年报（用户确认，50 份留后续补数据源）。

## 任务清单

- [x] M5.01 L3 M9 AgentOrchestrator ReAct 循环
      （agent/orchestrator.py + react_parser.py + prompts.py；Thought→Action→
        Observation 循环，步数上限 8、工具报错 3 次、解析失败 3 次终止；
        生成异常→generation_error 交任务级重试；真实 LLM 冒烟通过：
        工具自我修正科目名 + 计算同比正确 + 无编造）
- [x] M5.02 L3 M9 ToolRegistry + 6 个工具实现
      （tool_registry.py + tools/{query_statement,compute_yoy,compute_qoq,
        check_accounting,search_kb,unit_convert}；依赖注入面向
        StatementReader Protocol；业务性失败（无数据/未就绪）→ ok=True+
        data=null+reason 不消耗报错计数；search_kb 惰性连接 Milvus，
        知识库未构建时业务性提示）
- [x] M5.03 L3 M9 JSON Schema 强约束
      （废弃 outlines/guidance；system prompt 注入工具 JSON Schema +
        json_mode=True；react_parser 三级容错：严格 JSON → 代码块 →
        行级键提取；坏输出纠正重试）
- [x] M5.04 L3 M9 内部 SSE 端点 /internal/chat/stream
      （app/api/chat.py + schemas/chat.py；orchestrator 增 on_event 回调 +
        conversation 多轮历史；事件流 thought→tool_call→tool_result→
        token→done（spec §6.3.3）；整流超时 120s 兜底；真实 LLM 冒烟：
        3 步 ReAct 换路成功、同比 1.64% 正确、done 元数据完整；590 全绿）
- [x] M5.05 L2 问答 SSE 透传
      （ChatController/ChatService/ChatStreamProxy（WebClient 拉流）/
        ChatMessageProducer（chat.exchange 控制流）+ L3 ChatConsumer；
        会话 CRUD + 消息 SSE；token 累计答案落库 chat_message；MQ
        发布失败不阻断数据面（spec §3.3 混合模式）；真实冒烟：3 步
        ReAct 科目名自我修正 + 同比 1.64% 正确 + 双端落库 + 队列 ack）
- [x] M5.06 L2 会话上下文管理
      （SessionContextService：Redis Hash fin:chat:context:{id} 存最近 10 轮
        + 压缩摘要 + 轮数；第 11 轮起触发 L3 /internal/chat/compress 压缩，
        失败降级保留全量历史；ChatService 装配 summary 注入 L3 prompt +
        done 后 fire-and-forget 追加轮次；真实冒烟：11 轮后 rounds=11/
        history=20/summary 保留总资产 3038 亿等关键事实，第 12 轮回答
        正确引用窗口外摘要数据）
- [x] M5.07 L3 知识库构建脚本
      （chunker.py（512/64 句边界切块 + 表格行 TABLE_ROW/HEADER）+ build_kb.py
        + V9 kb_chunks 迁移；3 份 sample 入库 18736 chunks（TEXT 18307 +
        TABLE_ROW 412 + TABLE_HEADER 17），MySQL/Milvus 一致；search_kb
        迁移 MilvusClient + load_collection；真实冒烟：LLM 经 search_kb 命中
        产能表格行、区分 2025 新增与历史项目、回答引用第 15/98 页）
- [ ] M5.08 前端问答页
- [ ] M5.09 问答链路集成测试

## 阶段验收

- [ ] 5 个标准问题命中率 ≥ 0.85（Phase 1 未涉及，M5.09 验收）
- [ ] 首 token < 15s（M5.04 起可测）
- [ ] ReAct thought/tool_call 可视化正常（M5.04/5.08 起）
- [ ] 知识库预置 50 份年报（放宽为 3 份 sample；M5.07 构建）
