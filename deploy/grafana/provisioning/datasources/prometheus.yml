# M6.06 Grafana 数据源自动配置 — Prometheus。
apiVersion: 1

datasources:
  - name: Prometheus
    uid: finreport-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
